<!DOCTYPE html>
<html>
  <head>
    <title>All services all the time</title>
    <%= stylesheet_link_tag 'style'%>
  </head>
  <body>
    <%- flash.each do |name, msg| -%>
      <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
    <%- end -%>
    <%= yield %>
  </body>
</html>

